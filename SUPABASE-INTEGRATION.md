# 🦅 تكامل Supabase مع حورس نيوز

## ✅ تم تحديث المشروع بنجاح!

تم ربط المشروع بقاعدة بيانات Supabase بنجاح. الآن يمكنك إضافة أخبار جديدة من لوحة التحكم في Supabase وسيتم عرضها مباشرة على الموقع!

## 📋 بيانات الاتصال

```
SUPABASE_URL: https://krxqaokajfxtogtawuiq.supabase.co
SUPABASE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtyeHFhb2thamZ4dG9ndGF3dWlxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjgwNzU5MDEsImV4cCI6MjA4MzY1MTkwMX0.saAS-eKtoB4hMSjwe7m_Oq3r1L7vojjFfQI6CGEuPXc
```

## 🗄️ هيكل جدول الأخبار (news table)

يجب إنشاء جدول بإسم `news` في Supabase مع الأعمدة التالية:

| العمود | النوع | الوصف |
|--------|-------|-------|
| `id` | `bigint` | المفتاح الأساسي |
| `title` | `text` | عنوان الخبر |
| `summary` | `text` | ملخص الخبر |
| `content` | `text` | محتوى الخبر الكامل |
| `category` | `text` | التصنيف (politics, economy, sports, tech, culture, health, world) |
| `image_url` | `text` | رابط الصورة |
| `author` | `text` | اسم الكاتب |
| `views` | `bigint` | عدد المشاهدات (اختياري) |
| `is_breaking` | `boolean` | خبر عاجل؟ |
| `is_featured` | `boolean` | خبر مميز؟ |
| `is_published` | `boolean` | منشور؟ |
| `created_at` | `timestamp` | تاريخ الإنشاء |
| `updated_at` | `timestamp` | تاريخ التعديل |

## 🔐 إعدادات قاعدة البيانات

### تفعيل Realtime
تأكد من تفعيل Realtime للجدول `news`:
1. اذهب إلى Database > Tables > news
2. اضغط على "Realtime" في الأعلى
3. فعّل "Realtime" للجدول

### إعدادات الأمان (RLS)
تأكد من تفعيل Row Level Security:
1. Database > Tables > news > RLS
2. تفعيل "Enable RLS"
3. إضافة سياسات للقراءة العامة:

```sql
CREATE POLICY "Enable read access for all users"
  ON public.news
  FOR SELECT
  USING (true);
```

## 🚀 الميزات الجديدة

### 1️⃣ جلب البيانات تلقائياً من Supabase
- عند تحميل الصفحة، يتم جلب جميع الأخبار من قاعدة البيانات
- البيانات تُحفظ في `localStorage` كنسخة احتياطية

### 2️⃣ تحديث مباشر (Real-time)
- عند إضافة خبر جديد في Supabase، يظهر مباشرة على الموقع 🔴
- عند تعديل خبر، يتم تحديثه مباشرة
- عند حذف خبر، يختفي من الموقع مباشرة
- يظهر تنبيه "تم إضافة خبر جديد! ✨" عند إضافة خبر

### 3️⃣ عداد المشاهدات
- كل مرة تفتح خبراً، يزداد عدد المشاهدات بـ 1
- يتم حفظ المشاهدات في قاعدة البيانات

## 🔄 كيفية إضافة أخبار جديدة

### من خلال Supabase Dashboard:
1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى Database > news
4. اضغط "Insert row" أو "New row"
5. أملأ البيانات:
   - `title`: عنوان الخبر
   - `summary`: ملخص
   - `content`: المحتوى الكامل
   - `category`: اختر من القائمة
   - `image_url`: رابط صورة
   - `author`: اسم الكاتب
   - `is_breaking`: حدد إذا كان عاجل
   - `is_featured`: حدد إذا كان مميز
   - `is_published`: يجب أن يكون true لعرضه
6. اضغط "Save"
7. ستشاهد الخبر الجديد مباشرة على الموقع! 🎉

### من خلال API:
يمكنك استخدام الدوال المعرّفة في `js/supabase-config.js`:

```javascript
// إضافة خبر جديد
await HorusDB.createNews({
    title: "عنوان الخبر",
    summary: "الملخص",
    content: "المحتوى الكامل",
    category: "politics",
    image_url: "https://example.com/image.jpg",
    author: "اسم الكاتب",
    is_breaking: false,
    is_featured: true,
    is_published: true
});

// جلب جميع الأخبار
const allNews = await HorusDB.fetchAllNews();

// البحث
const results = await HorusDB.searchNews("كلمة البحث");

// حذف خبر
await HorusDB.deleteNews(newsId);

// تحديث خبر
await HorusDB.updateNews(newsId, { title: "العنوان الجديد" });
```

## 📱 الملفات المحدثة

✅ **index.html** - الصفحة الرئيسية
- إضافة مكتبة Supabase
- جلب البيانات من Supabase
- تفعيل Real-time subscriptions

✅ **article.html** - صفحة تفاصيل الخبر
- جلب بيانات الخبر من Supabase
- تحديث عداد المشاهدات

✅ **search.html** - صفحة البحث
- جلب البيانات من Supabase
- البحث في البيانات

✅ **js/supabase-config.js** - إعدادات Supabase
- تم تحديث بيانات الاتصال

✅ **gallery.html** و **videos.html**
- إضافة مكتبة Supabase (استعداداً للتطويرات المستقبلية)

## 🧪 الاختبار

1. افتح الموقع في المتصفح
2. ستشاهد رسالة في console: `✅ Horus News Ready!`
3. أضف خبر جديد من Supabase Dashboard
4. ستشاهد الخبر الجديد يظهر مباشرة! 🎉

## ⚙️ التحكم في الأخبار

### مثال على JSON يمكنك إدراجه مباشرة:

```json
{
  "title": "اكتشاف طبي جديد يحدث ثورة في الطب",
  "summary": "علماء يتوصلون إلى علاج جديد...",
  "content": "<p>محتوى الخبر الكامل...</p>",
  "category": "health",
  "image_url": "https://images.unsplash.com/...",
  "author": "د. أحمد محمد",
  "views": 1000,
  "is_breaking": true,
  "is_featured": true,
  "is_published": true,
  "created_at": "2026-01-13T12:00:00Z"
}
```

## 📞 المشاكل والحلول

### المشكلة: لا تظهر الأخبار
**الحل:**
1. تحقق من بيانات الاتصال
2. تأكد من وجود جدول `news` في Supabase
3. تأكد من أن `is_published = true`
4. افتح Developer Console (F12) وتحقق من الأخطاء

### المشكلة: Real-time لا يعمل
**الحل:**
1. تأكد من تفعيل Realtime في Database > news
2. أعد تحميل الصفحة
3. تحقق من الاتصال بالإنترنت

### المشكلة: صور لا تظهر
**الحل:**
- استخدم روابط صور كاملة (يبدأ بـ https://)
- تأكد من أن الصورة متاحة عامة

## 🎓 موارد إضافية

- [Supabase Docs](https://supabase.com/docs)
- [JavaScript Client Library](https://supabase.com/docs/reference/javascript)
- [Real-time Subscriptions](https://supabase.com/docs/guides/realtime)

---

**تم التحديث بنجاح! 🎉**
استمتع بإدارة أخبارك بسهولة من خلال Supabase!
